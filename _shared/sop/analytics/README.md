# SOP: analytics

## 何を書く場所か
週次・月次の数値取得・分析・レポート作成の具体的な実行手順。

## 書いてはいけないもの
分析理論そのもの(→`_shared/knowledge/analytics/`)、実際のKPI実績データそのもの(→事業の`data/`・`reports/`)。

## 誰が参照するか
`ig-analytics`。

## 更新ルール
指標定義・レポート形式が変わったら更新する。

## 関連SOP
- `publishing/`
- `research/`
